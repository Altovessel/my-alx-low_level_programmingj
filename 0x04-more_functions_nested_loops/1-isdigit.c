#include "main.h"

/**
 * _isdigit: Checks if c is a digit
 * @c: The character to be checked
 *
 * Return: 1 if c is a digit, 0 if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
