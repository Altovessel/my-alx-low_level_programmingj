#include "main.h"

/**
 * print_numbers - prints numbers from 0 to 9
 *
 * @a - parameter to use
 *
 * Return - No return
 */
void print_numbers(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		_putchar(a);
	}
		_putchar(\n);
}
